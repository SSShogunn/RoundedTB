name: CI / Release

on:
  push:
    branches: [ master ]
    tags: [ 'v*.*.*' ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:
    inputs:
      create_release:
        description: 'Create a GitHub Release?'
        required: true
        default: 'false'
        type: choice
        options: [ 'true', 'false' ]
      tag_name:
        description: 'Release tag (e.g. v1.0.0) — required if creating a release'
        required: false
        type: string

jobs:
  build:
    name: Build & Release
    runs-on: windows-2022
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 9
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Restore
        run: dotnet restore RoundedTB/RoundedTB.csproj

      - name: Build
        run: dotnet build RoundedTB/RoundedTB.csproj -c Release --no-restore

      - name: Resolve release tag
        if: startsWith(github.ref, 'refs/tags/v') || inputs.create_release == 'true'
        id: tag
        run: |
          $t = if ("${{ github.ref_name }}" -ne "master") { "${{ github.ref_name }}" } else { "${{ inputs.tag_name }}" }
          echo "value=$t" >> $env:GITHUB_OUTPUT

      - name: Zip build output
        if: startsWith(github.ref, 'refs/tags/v') || inputs.create_release == 'true'
        run: Compress-Archive -Path "RoundedTB/bin/Release/net9.0-windows10.0.22621.0/*" -DestinationPath RoundedTB-${{ steps.tag.outputs.value }}.zip

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/v') || inputs.create_release == 'true'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.value }}
          files: ./RoundedTB-${{ steps.tag.outputs.value }}.zip
          generate_release_notes: true
